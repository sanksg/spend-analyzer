# --- Security ---
# Google Gemini API Key (Required)
# Get one here: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_api_key_here

# --- Database ---
# Connection string (Defaults to local SQLite if omitted)
DATABASE_URL=sqlite:///./data/spend.db

# --- AI Models ---
# Model used for parsing PDF statements (Fast/Cheap recommended)
GEMINI_MODEL=gemini-1.5-flash

# Model used for "Ask Your Data" analysis (Smarter capability recommended)
ANALYSIS_MODEL=gemini/gemini-1.5-flash

# --- Processing Limits ---
# Max pages to process per PDF
MAX_PDF_PAGES=50
# Timeout for parsing in seconds
PARSE_TIMEOUT_SECONDS=120

# --- Server ---
# CORS Origins (Comma separated list)
CORS_ORIGINS=["http://localhost:5173", "http://127.0.0.1:5173"]
